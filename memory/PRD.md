# El Foro In Oregon — PRD

## Problem Statement
Plataforma web informativa, no partidista y en español para la comunidad hispanohablante de Oregon. Ayuda a responder: ¿Qué está pasando en Oregon? ¿Qué recursos existen para mí? ¿Qué leyes debo conocer? ¿Qué debo conocer sobre Oregon? Identidad editorial/institucional (verde profundo, charcoal, blanco, tonos tierra; tipografía Lora + Work Sans).

## Architecture
- **Frontend**: React 19 + React Router 7 + Tailwind + shadcn/ui. Public site (PublicLayout) + Admin CMS (/admin, JWT protected).
- **Backend**: FastAPI modular (core, auth, content, elections, ai, storage, seed_data). All routes under /api.
- **DB**: MongoDB. Collections: users, articles, resources, oregon_info, places, elections, races, candidates, media, searches, analytics.
- **Auth**: JWT Bearer (localStorage `foro_token`). Roles: contributor < writer < editor < admin < super_admin. Publishing requires editor+.
- **AI**: GPT-5.4 via emergentintegrations (EMERGENT_LLM_KEY; supports own OPENAI_API_KEY via env). Editorial non-partisan system prompt. AI content forced to draft/needs_review.
- **Storage**: Emergent Object Storage for media library.

## User Personas
- Community reader (Spanish-speaking Oregon resident) — reads news, finds resources, understands laws, compares candidates.
- Editorial team (writer/editor/admin) — manages content via CMS with AI assistant.

## Core Requirements (static)
5 public sections (Inicio, Entérate, Recursos, Oregon Te Informa, Conoce Oregon) + Elecciones (subsection, neutral candidate comparison, "El Foro Pregunta"). Full CMS, AI editorial assistant, sources, corrections, global search, filters, localization (cities/counties), demo content marked DEMO.

## Implemented (2026-06)
- ✅ Public site: Home (hero+search, featured, resources grid, Oregon Te Informa, Conoce Oregon, Elecciones banner), 4 list pages with category/search filters, 4 detail page types, elections list + side-by-side compare + candidate detail, global search grouped by type, 6 institutional static pages, dark/light theme.
- ✅ Backend: JWT auth + roles + user management, generic content CRUD with workflow states + slug + views, elections/races/candidates + questions applied to race, AI assist (18+ actions incl. structured explain_law/extract_resource/tags/generate_questions), media upload/serve/list, search + analytics tracking, dashboard stats, config taxonomies.
- ✅ Admin CMS: dashboard with stats, generic list+editor per content type, AI Assistant dialog, media library, elections manager, race/candidate editors, users & roles.
- ✅ Demo content seeded (3 articles, 3 resources, 3 oregon-info, 3 places, 1 election / 2 races / 4 candidates, all DEMO).
- ✅ Testing: 23/23 backend pytest pass; frontend flows verified.
- ✅ AI "Investigar con AI" flow (2026-06): AIResearch component integrated into ContentEditor for all kinds (articles/resources/oregon-info/places). Admin types a topic → AI returns 4 distinct options → picks one → full draft fills editor fields (used_ai=true, status=draft) → optional copyright-free AI image (gpt-image-1) applied to featured_image. Backend endpoints /api/ai/research, /api/ai/generate-post, /api/ai/image. Verified E2E (testing iteration_2: 6/6 frontend behaviors pass).
- ✅ Site Settings + batch drafts (2026-06): New admin section "Configuración del sitio" (/admin/settings, editor+) to edit homepage hero (eyebrow, title, subtitle, search label, background image) with live preview. AI buttons: "Generar textos con AI" (POST /ai/hero-text → 4 options) and "Generar imagen con AI" (POST /ai/image, copyright-free). Persisted in db.settings singleton via GET /api/site-settings + PUT /api/admin/site-settings; Home.jsx consumes it live. Also: "Investigar con AI" now has "Crear borradores de todas" to batch-create a draft per research option at once. Verified E2E (testing iteration_3: 7/7 pass).
- ✅ Color palettes + section texts + mobile preview (2026-06): Palette picker in /admin/settings with 6 presets (green/ocean/wine/indigo/teal/earth) applied site-wide via CSS vars (lib/palettes.js applyPalette + ThemeApplier in App.js), live preview + persistence (settings.palette). Editable section texts for "Recursos" and "Oregon Te Informa" (title+description) reflected on Home. Desktop/Mobile hero preview toggle in settings. Verified E2E (testing iteration_4: 100% pass).
- ✅ Hero eyebrow contrast fix (2026-06): removed `.eyebrow` class (forced dark text-primary) on Home + Elecciones heroes, using explicit light color; readable across all palettes (~11:1 contrast). Verified iteration_5.
- ✅ AI content quality overhaul (2026-06): rewrote EDITORIAL_SYSTEM_PROMPT + NO_INVENT + research/generate-post prompts in ai.py to enforce CONCRETE, useful, BRIEF output (articles ~450 words / 3-6 short paras), ban filler & editor-directed meta-commentary, never invent names/data, and use editor-provided facts/links as grounding. Added GEN_GUIDE per kind. AIResearch instructions field is now a Textarea to paste real facts/links. Verified via curl: article dropped from ~1400 fluffy words to 332 concrete/actionable words. NOTE: only affects newly generated content; pre-existing drafts must be regenerated or shortened via AIAssistant.
- ✅ Real web research (2026-06): /api/ai/research + /api/ai/generate-post now use Gemini (gemini-2.5-flash) with Google Search grounding (.with_tools googleSearch) via EMERGENT_LLM_KEY. Returns real results with source links; citations mapped to `sources` ({name,url,...}) rendered by the Sources component. AIResearch shows source links per option + "Crear borradores de todas".
- ✅ AI illustrations (2026-06): "Ilustrar con AI" button in ContentEditor image field. /api/ai/illustrate crafts a scene from title/summary then generates via gpt-image-1 (quality=high→medium, size 1536x1024 landscape). STYLE_PREFIX: comic (modern color comic, natural palette — user-tuned), illustration, photo. Default=comic.
- ✅ Image performance (2026-06): all AI + uploaded images compressed to WebP (quality 82, max 1600px) via storage.compress_image. Existing images migrated via POST /api/media/optimize-existing (28 imgs, 78MB→6.8MB). Long Cache-Control.
- ✅ cPanel deploy kit (2026-06): /deploy.sh + /bootstrap.sh + /deploy/ (install_server.sh, fix.sh, restart.sh, setup-autostart.sh, htaccess port 8008, requirements.prod.txt w/ Pillow + emergentintegrations unpinned, backend.env.production.example, README.md). Target: GoDaddy VPS + cPanel + AlmaLinux + Apache + Py3.9 + Node18 + Mongo local. Config: user=elfaroinoregon, port=8008, repo=github.com/Pzsuave007/elfaro, domain=elfaroinoregon.com. Added GET /api/ health route, SEED_DEMO env flag.
- ✅ Content transfer (2026-06): deploy/content_export/*.json (9 articles, 6 resources, 5 oregon_info, 8 places, settings, 47 media) + media_files/ (real image bytes, 11MB). deploy/import_content.py upserts by id + copies images to MEDIA_DIR.
- ✅ Missing-images fix + de-brand (2026-06): storage.py now supports MEDIA_DIR local disk store (put/get/serve read+write disk first, Emergent fallback) so images don't depend on EMERGENT_LLM_KEY namespace on their server. Cleaned public/index.html: title 'El Foro In Oregon', lang es, removed Emergent script + PostHog analytics. Verified E2E (iteration_8: 100%, images served from disk, tab title clean).
- ✅ Sponsors/Patrocinadores (2026-06): backend sponsors.py (public list active+tier-sorted, click tracking call/directions/website, sponsor-leads form, GET /admin/sponsor-leads). sponsors added to COLLECTIONS (generic admin CRUD via /admin/sponsors + schemas.js with tier select oro/plata/bronce, logo, phone, address, dates). Public: components/Sponsors.jsx (SponsorStrip on Home + footer, SponsoredBy in ArticleDetail), pages/Aliados.jsx (/aliados) with sponsor cards (Llamar/Cómo llegar/Sitio web CTAs tracked) + "Sé nuestro aliado" lead form. sponsors_router registered BEFORE content_router (avoids /public/{kind} shadowing). Self-tested via curl + screenshot. GAP: no admin UI yet to VIEW captured leads (stored in db.sponsor_leads, readable via GET /admin/sponsor-leads).

- ℹ️ Sponsor "Tipo de contenido no válido" (2026-06): NOT a code bug. Preview verified working (create/upload logo/publish sponsor all OK). Root cause: user's production VPS is running an older build (pre-Sponsors) — `sponsors` not in deployed COLLECTIONS. Resolution = redeploy VPS: "Save to Github" → on VPS as root `cd /home/elfaroinoregon/repo && git pull && bash deploy.sh` (fix.sh copies backend/*.py incl sponsors.py + rebuilds frontend + restarts backend). No code change made.

- ✅ Anuncio lateral flotante "Apoya negocios locales" (2026-06): nuevo componente SponsorAd (components/Sponsors.jsx) — tarjeta sticky con logo, nombre, nivel, descripción y botones Llamar / Cómo llegar / Sitio web (con tracking). Rota aleatoriamente entre patrocinadores activos; si no hay, muestra CTA "¿Tu negocio aquí? Sé nuestro aliado" → /aliados. Añadido a la barra flotante de las 4 páginas de detalle: ArticleDetail y OregonInfoDetail (reestructuradas a grid lg:grid-cols-3 con aside sticky, max-w-5xl), ResourceDetail y PlaceDetail (aside envuelto en contenedor sticky con SponsorAd debajo de la tarjeta existente). Logos de SponsorStrip agrandados (oro h-20/24, plata h-14/16, bronce h-11/12). Verificado por screenshot en Historias y Recursos.
- 🐞 Fix bug sort patrocinadores (2026-06): /api/public/sponsors devolvía 500 (TypeError str<int) cuando el campo `order` mezclaba string vacío y número. sponsors.py ahora usa _order_val() que castea a float con default 0. Verificado por curl (5 items ok).
- 🚀 Deploy prod (2026-06): resuelto error "Tipo de contenido no válido" en VPS — NO era bug de código; el servidor corría build viejo. Causa del arranque fallido: usuario cPanel usa jailshell → hay que reiniciar uvicorn con `su -s /bin/bash -l`. Fix subida de logo: media_store era de root (PermissionError) → chown al usuario cPanel. deploy/repair.sh reescrito como actualizador 1-comando (git reset --hard origin, pip, cp backend, rebuild frontend, chown media_store, restart via restart.sh, verify).

- ✅ Patrocinio por sección y sub-sección (2026-06): nuevos campos en sponsor `feature_section` (select: articles/resources/oregon-info/places o "none") y `feature_category` (texto, opcional, ej "Vivienda"). Nuevo componente SectionSponsorBanner (components/Sponsors.jsx) inyectado en ContentListPage arriba (entre hero y filtros) para las 4 secciones. Prioridad: patrocinador de sub-sección (match feature_category == category||group activo, case-insensitive) > patrocinador de sección (feature_section == kind, sin categoría). Banner premium con logo/nombre/summary + CTAs Llamar/Cómo llegar/Sitio web (tracked). Si no hay patrocinador → CTA sutil "Patrocina la sección de X — Sé nuestro aliado" → /aliados (herramienta de venta). Verificado por screenshot (Recursos con sponsor, Historias con CTA vacío) + curl.

## Backlog / Remaining (P1/P2)
- P1: Rich text editor (currently textarea), scheduled auto-publish job, SEO meta tags injection + sitemap.xml/robots.txt served, ballot measures.
- P2: Newsletter, WhatsApp, push, member accounts, saved articles, English version, "Who Represents Me" + ZIP lookup, PWA. (Architecture ready.)

## Test Credentials
admin@elforo.org / ForoOregon2026 (super_admin). See /app/memory/test_credentials.md.
